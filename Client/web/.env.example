# Backend API URL (Render URL in production, empty for local dev proxy)
VITE_API_URL=https://your-render-app.onrender.com

# Set to "true" ONLY for offline UI dev. This bypasses real auth and must stay false in production.
# VITE_DEV_AUTH=false
